import React from 'react';

export function DayDummy() {
    // HACK: This should have no content, but it somehow would be out of
    // alignment if so.
    return <div className="dayDummy">&nbsp;</div>
}
